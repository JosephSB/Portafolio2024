---
import { STORGE_BLOBS } from "../config";
interface Props {
  id: string;
  image: string;
  title: string;
  subtitle?: string;
  skills: string[];
  className?: string;
  demo?: string;
  Repositorio?: string;
  /** Muestra el overlay siempre encendido */
  alwaysOverlay?: boolean;
  /** URL destino. Si no se pasa, va a la página de detalle interno /products/{id}. */
  href?: string;
}

const {
  id,
  title,
  subtitle,
  skills,
  image,
  className = "",
  alwaysOverlay = false,
  href,
} = Astro.props;
const linkHref = href ?? `/products/${id}`;
---

<a
  class={`card-sideproject ${className} ${alwaysOverlay ? "always-overlay" : ""}`}
  href={linkHref}
>
  <div class="container">
    <img src={`${STORGE_BLOBS}${image}`} alt={title} loading="lazy" />
    <div class="card-project-float">
      <div class="content">
        <p class="title">{title}</p>
        {
          subtitle && (
            <p class="subtitle">
              {subtitle.length > 120
                ? subtitle.slice(0, 120) + "..."
                : subtitle}
            </p>
          )
        }
        <ul class="list-skills">
          {
            skills.map((item, index) => (
              <li>
                {index !== 0 ? "• " : ""}
                {item}
              </li>
            ))
          }
        </ul>
      </div>
    </div>
  </div>
</a>

<style>
  .card-sideproject {
    width: 100%;
    height: 100%;
    position: relative;
    cursor: pointer;
    border-radius: 8px;
    display: block;
    overflow: hidden;
  }

  .card-sideproject .container {
    width: 100%;
    height: 100%;
  }

  .card-sideproject img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    border-radius: 8px;
  }

  .card-project-float {
    border-radius: 8px;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    padding: 1rem;
    display: flex;
    opacity: 0;
    justify-content: flex-end;
    align-items: flex-start;
    flex-direction: column;
    background: linear-gradient(
      rgba(0, 0, 0, 0.1) 10%,
      rgba(0, 0, 0, 0.85) 80%
    );
    transition: opacity 300ms ease-in-out 0s;
  }

  .card-sideproject.always-overlay .card-project-float {
    opacity: 1;
  }

  .card-sideproject:hover .card-project-float {
    opacity: 1;
  }

  .card-sideproject .title {
    font-weight: bold;
    color: white;
    margin: 0;
  }

  .card-sideproject .subtitle {
    color: white;
    margin: 4px 0 0;
    font-size: 12px;
    line-height: 1.4;
  }

  .card-sideproject .list-skills {
    padding: 0;
    color: white;
    display: flex;
    gap: 5px;
    list-style: none;
    margin: 0.5rem 0 0;
    font-size: 0.7rem;
    flex-wrap: wrap;
  }
</style>
